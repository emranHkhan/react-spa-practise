import React from 'react';
import './header.css'
const Header = () => {
    return (
        <div className="main-container">
            <nav className="nav-container">
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
            </nav>
        </div>
    );
};

export default Header;